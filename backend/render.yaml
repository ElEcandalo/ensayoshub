services:
  - type: web
    name: ensayohub-backend
    env: node
    buildCommand: pnpm install && pnpm build
    startCommand: pnpm start
    plan: free
    autoDeploy: false
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000